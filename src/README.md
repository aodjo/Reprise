# Source projects

This directory contains the shared cross-platform application and its
operating-system implementations. The existing native Xcode application lives
under `Reprise.Platform.MacOS` while the shared core and desktop projects are
being introduced. See [the migration document](../docs/CROSS_PLATFORM.md) for
ownership boundaries.
