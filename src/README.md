# Source projects

This directory contains the shared cross-platform application and its
operating-system implementations. The existing native Xcode application lives
under `Reprise.Platform.MacOS`. The shared .NET core, Avalonia desktop UI, and
the first MPRIS-based Linux host now live alongside it. See
[the migration document](../docs/CROSS_PLATFORM.md) for ownership boundaries.
